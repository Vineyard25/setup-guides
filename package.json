{
  "name": "v25-tech-setup-checklist",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/input.css -o ./dist/tailwind.css --minify",
    "build:babel": "npx babel index.html --out-file dist/index.html --presets=@babel/preset-react",
    "replace-cdn": "sed '/cdn.tailwindcss.com/d' index.html | sed '/@babel\\/standalone/d' | sed 's|</head>|  <link rel=\"stylesheet\" href=\"./tailwind.css\" />\\n</head>|' > dist/index.html",
    "minify": "npx html-minifier-terser dist/index.html -o dist/index.html --collapse-whitespace --remove-comments --minify-js true --minify-css true",
    "build": "npm run build:css && npm run replace-cdn && npm run build:babel && npm run minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "html-minifier-terser": "^7.2.0"
  }
}
